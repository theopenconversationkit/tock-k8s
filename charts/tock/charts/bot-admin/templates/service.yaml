apiVersion: v1
kind: Service
metadata:
  name: {{ include "bot-admin.fullname" . }}
  labels:
    {{- include "bot-admin.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "bot-admin.selectorLabels" . | nindent 4 }}
